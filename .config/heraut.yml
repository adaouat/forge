# yaml-language-server: $schema=https://raw.githubusercontent.com/adaouat/heraut/v0.58.0/schema.json

version: "1"

versioning:
  strategy: semver
  tag_prefix: v

changelog: {}

release: {}

forge:
  - name: GitHub
    platform: github
